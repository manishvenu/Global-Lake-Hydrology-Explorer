# Discussion: Sep 20, 2023

## Status & Updates

- Demo is done! To run:

  ```bash
  cd "Global Lake Hydrology Explorer Directory"
  python -m GLHE.ngrok_demo
  # then start ngrok with:
  ngrok start --all   # opens ports 8500 and 5000
  ```

- Next step: make the demo flexible, interactive, fast, and aligned with the project goals.
- Goal: help users understand where the data comes from and how it is used.
